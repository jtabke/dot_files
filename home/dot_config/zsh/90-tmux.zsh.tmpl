# tmux attachment is opt-in for local and SSH interactive terminals.
{{- if and .autoTmux (eq .role "workstation") }}
if [[ -o interactive && -t 1 && -z "${TMUX-}" && -z "${CI-}" ]] && command -v tmux >/dev/null 2>&1; then
  tmux new-session -A -s default
fi
{{- end }}
