# Shared terminal behavior.
desktop-notifications = true
font-size = 9
background-opacity = 0.8

# Keep mouse selections, Neovim's + register, and the system clipboard aligned.
copy-on-select = clipboard
clipboard-read = deny
clipboard-write = allow
